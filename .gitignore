# OS
.DS_Store
Thumbs.db

# Editors / tooling
.claude/
.vscode/
.idea/

# Large source data / scratch (derived files are committed; raw dumps are not)
*.zip
*.har

# Node (not used today, but just in case)
node_modules/
